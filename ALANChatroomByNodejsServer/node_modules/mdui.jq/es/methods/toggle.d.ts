import './each';
import './hide';
import './show';
declare module '../JQ' {
    interface JQ<T = HTMLElement> {
        /**
         * 切换集合中所有元素的显示状态
         * @example
    ```js
    $('.box').toggle()
    ```
         */
        toggle(): this;
    }
}
