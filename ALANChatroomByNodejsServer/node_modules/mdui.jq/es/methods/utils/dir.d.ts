import { JQ } from '../../JQ';
import '../each';
import '../is';
export default function dir($elements: JQ, nameIndex: number, node: 'parentNode' | 'nextElementSibling' | 'previousElementSibling', selector?: any, filter?: string): JQ;
