import './eq';
declare module '../JQ' {
    interface JQ<T = HTMLElement> {
        /**
         * 返回当前集合中最后一个元素的 JQ 对象
         * @example
    ```js
    $('div').last()
    ```
         */
        last(): this;
    }
}
