import './eq';
declare module '../JQ' {
    interface JQ<T = HTMLElement> {
        /**
         * 返回当前集合中第一个元素的 JQ 对象
         * @example
    ```js
    $('div').first()
    ```
         */
        first(): this;
    }
}
