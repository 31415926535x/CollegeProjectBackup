import './css';
import './eq';
import './offset';
import './offsetParent';
interface Coordinates {
    left: number;
    top: number;
}
declare module '../JQ' {
    interface JQ<T = HTMLElement> {
        /**
         * 获取集合中第一个元素相对于父元素的偏移
         * @example
    ```js
    $('.box').position();
    // { top: 20, left: 30 }
    ```
         */
        position(): Coordinates;
    }
}
export {};
